# Cloudflare Workers Configuration
# This file configures your worker deployment

name = "bitcraft-market-proxy"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

# Workers Free Tier:
# - 100,000 requests/day
# - No credit card required
# - Perfect for this use case

# Your worker is available at:
# https://bitcraft-market-proxy.jbaird-cb6.workers.dev
#
# This worker now only provides CORS proxy functionality.
# Market monitoring is handled by Supabase (via local-monitor.js).

# Migration to remove Durable Objects
[[migrations]]
tag = "v2"
deleted_classes = ["MarketMonitor"]
